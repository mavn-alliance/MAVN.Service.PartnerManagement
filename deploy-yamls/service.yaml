apiVersion: v1
kind: Service
metadata:
  labels:
    app: partnermanagement
    environment: Development
  name: partnermanagement
  namespace: services
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: partnermanagement
  sessionAffinity: None
  type: ClusterIP
